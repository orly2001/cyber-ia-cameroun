# --- Core / contrats ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Base de données ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9      # PostgreSQL (prod)

# --- Bloc 1 : scan vulnérabilités ---
python-nmap>=0.7.1
python-owasp-zap-v2.4>=0.0.21
requests>=2.31            # enrichissement CVE/NVD

# --- Blocs 2-3 : IA / NLP ---
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
# BERT multilingue (lourd — installer au besoin) :
transformers>=4.40
torch>=2.2

# --- Bloc 5 : API + dashboard ---
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-multipart>=0.0.9   # requis par POST /api/upload (UploadFile)

# --- Tests ---
pytest>=8.0
